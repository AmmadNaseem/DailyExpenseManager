import { MonthsToNumberPipe } from './months-to-number.pipe';

describe('MonthsToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthsToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
